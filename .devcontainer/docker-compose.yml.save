version: '3'
services:
  app:
    build: .
    volumes:
      - ..:/workspaces:cached
    command: sleep infinity
    ports:
      - "3000:3000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo:6.0
    restart: always
    ports:
      - "27017:27017"


